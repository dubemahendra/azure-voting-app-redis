pipeline {
    agent any

    stages {
        stage('Verify Banch') {
            steps {
                echo "$GIT_BRANCH"
            }       
     stages {
        stage('Hello World') {
            steps {
                echo "Hello World"
            } 
        }
    }
}
